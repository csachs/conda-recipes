{% set name = "vispy-pyside2" %}
{% set version = "0.0.1.dev1" %}
{% set sha256 = "" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  git_rev: df0f93921a3bf9553d3e4b624e3cd78043bae694
  git_url: https://github.com/csachs/vispy
  # fn: {{ name }}-{{ version }}.tar.gz
  # url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  # sha256: {{ sha256 }}

build:
  number: 1
  script: python setup.py install --single-version-externally-managed --record record.txt
  noarch: python

requirements:
  build:
    - python
    - setuptools
    - numpy
  run:
    - python
    - numpy

test:
  imports:
    - vispy

about:
  home: http://vispy.org/
  license: BSD 3-Clause
  license_family: BSD
  summary: 'VisPy is a high-performance interactive 2D/3D data visualization library.'
  description: |
    VisPy is a high-performance interactive 2D/3D data visualization library.
  doc_url: http://vispy.org/
  dev_url: http://vispy.org/

extra:
  recipe-maintainers:
    - csachs

